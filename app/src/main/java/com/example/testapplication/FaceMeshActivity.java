package com.example.testapplication;

import android.os.Bundle;
import android.view.Display;
import android.widget.ImageView;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;
import androidx.constraintlayout.widget.ConstraintLayout;

public class FaceMeshActivity extends AppCompatActivity {
    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_facemesh);

//        BitMapMeshView view = new BitMapMeshView(this);
//
//        Display displayDevice=getWindowManager().getDefaultDisplay();
//        int wid=displayDevice.getWidth();
//        int hit=displayDevice.getHeight();
//
//        view.setLayoutParams(new LinearLayout.LayoutParams(wid, hit));

    }
}
